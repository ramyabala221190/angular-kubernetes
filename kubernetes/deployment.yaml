apiVersion: apps/v1
kind: Deployment
metadata:
 name: angular-kube-deployment
 labels:
  app: angular-kube-app

spec:
  selector:
    matchLabels:
      app: angular-kube-pod

  template:
    metadata:
      labels:
        app: angular-kube-pod

    spec:
     containers:
       - name : angular-dev-kube-container
         image: angular-kube-image
         ports:
          - containerPort : 80
         resources: {}
         envFrom :
           - configMapRef:
               name : dev-app-config
#we are loading all environment variables from the config map with name :dev-app-config

